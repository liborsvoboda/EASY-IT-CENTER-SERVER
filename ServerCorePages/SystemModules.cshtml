@page "/SystemModules"

@* @{ Layout = "/ServerCorePages/Shared/SystemModulesLayout.cshtml"; } Ma nastaven Vice layoutu *@
@* @addTagHelper *, WebOptimizer.Core *@
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using System.Security.Claims
@using EasyITCenter.Controllers
@using EasyITCenter.Classes
@using EasyITCenter.DBModel
@using EasyITCenter.ServerCoreStructure
@using Microsoft.AspNetCore.Html
@using ServerCorePages
{
    string? sourcePath = HttpContext.Items.FirstOrDefault(a => a.Key.ToString() == "RequestedModulePath").Value?.ToString();
    ServerModuleAndServiceList? systemModule = ((ServerModuleAndServiceList?)HttpContext.Items.FirstOrDefault(a => a.Key.ToString() == "SystemModule").Value);
    ServerWebPagesToken? webpagestoken = ((ServerWebPagesToken?)HttpContext.Items.FirstOrDefault(a => a.Key.ToString() == "ServerWebPagesToken").Value);


   
}



@* @(Html.Raw(this.Model.GetManagedJsScriptsForLayout().ViewName)) *@

@* 
    udelat treevie jednoduche prochazeni souboru pomoci pathinfo a je to
    muze byt napsane v systemu staci 1x save file API pro ulozeni s def path


    Modul Dokaze nacist externi soubor a zracovat s nim 
    Vhodny pro editory atd, dodelat metody a umi to vse
*@
@* 
@if (serverModule != null) {
    @Html.Raw(serverModule.CustomHtmlContent);
} 
@if (this.BodyContent != null) {
    Html.Raw(this.BodyContent);
} *@



